kind: Deployment
apiVersion: apps/v1
metadata:
   name: mydeployments
spec:
   replicas: 3
   selector:     
    matchLabels:
     name: deployment
   template:
     metadata:
       name: testpod
       labels:
         name: deployment
     spec:
      containers:
        - name: container1
          image: ubuntu
          command: ["/bin/bash", "-c", "while true; do echo Hello, Sumant. Hope you are doing fine; sleep 5; done"]